# Docker Compose reads .env. The Python CLI uses exported environment variables.
# Generate locally: python -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_ACCESS_TOKEN=
# Exact HTTPS origin of your reverse proxy, without a path. Blank for loopback use.
PUBLIC_BASE_URL=
